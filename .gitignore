# Dependencies
node_modules/

# Build output
dist/

# Test coverage
coverage/
.nyc_output/

# Local development & IDE state
.claude/
.vscode/settings.json
.vscode/tasks.json

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Environment files
.env
.env.local
.env.*.local

# User project data (real carrefour exports — never commit)
/*.cmpx
/*.bn
/Car *.json
/RSI_*.cmpx
# Documents de travail déposés à la racine pour lecture (dossiers de carrefour,
# pièces client). Non couverts avant le 2026-08-12, alors qu'un `git add -A`
# suffisait à les envoyer dans l'historique.
/*.pdf
/*.docx
/*.xlsx
/samples/
/bn/

# Export JPEG de la vignette de partage, produit pour le téléversement manuel
# dans les réglages GitHub. Les vignettes du dépôt sont les .png (docs/ et
# public/), seules référencées par index.html.
/docs/social-preview.jpg

# Dev-only temporary artifacts
/package_new.json
/bn_viewer.html
/tmpclaude-*
/.tmp-anonymisation/
# Données de travail (originaux clients non anonymisés, dumps web)
/examples/
